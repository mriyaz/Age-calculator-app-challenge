export type DateObject = { day: number, month: number, year: number };
export type ErrorsObject = { day: string, month: string, year: string, dateInvalidorFuture: string };
export type AgeObject = { days: number, months: number, years: number };
